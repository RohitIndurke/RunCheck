/**
 * fix.ts — stub fixer for v0.1
 *
 * In v0.2 this will execute suggested commands. For now it just prints a notice.
 */

import pc from 'picocolors';

export function runFix(): void {
  console.log('');
  console.log(pc.yellow('⚠  --fix is not yet implemented.'));
  console.log(pc.dim('   Auto-fixing is coming in RunCheck v0.2.'));
  console.log(pc.dim('   For now, copy the "Suggested Fix" commands from the table above.'));
  console.log('');
}
